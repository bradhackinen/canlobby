
# Set project data directory (note: Raw data should be placed in data_dir/raw_data)
data_dir = '/path/to/data/here'
nama_device = 'cpu' # Set to e.g. "cuda:0" for gpu
nama_fp16 = False # nama_fp16=True speeds up embeddings.predict(), but requires hardware support